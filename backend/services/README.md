# backend/services

Purpose:
- Service layer components that implement business processes, orchestration, and integrations (databases, queues, external APIs).

Current state:
- Directory present; used by `backend/main.py` and other modules to run background or request-driven service logic.

How to use:
- Review the service implementations and dependency wiring in `backend/main.py`.

Notes / next steps:
- Document each service, its configuration, and required external dependencies.
