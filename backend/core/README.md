# backend/core

Purpose:
- Core domain logic, models and shared utilities used across the backend.

Current state:
- Directory present; contents should include core business logic referenced by other packages.

How to use:
- Import the core modules from other backend packages (e.g. `from backend.core import ...`).
- Add examples of important classes/functions used by the rest of the system.

Notes / next steps:
- Add usage examples and document configuration or environment variables required by core pieces.
