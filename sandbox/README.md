# Sandbox

This folder contains experiments and quick prototypes used during development.

Current state:
- `test_settings.py` for ad-hoc tests or configuration checks.
- `labelstudio/` and `minio/` subdirectories that appear to hold sandboxed integrations.

How to use:
- Sandbox contents are experimental. Do not depend on them for production code.
- Run individual scripts or tests in isolation.
