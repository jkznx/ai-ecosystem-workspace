# docs/api

Purpose:
- Stores API documentation artifacts and OpenAPI exports.

Current state:
- Directory exists for organizing API docs. Use `scripts/export_openapi.py` to populate or refresh files.

How to use:
- Place exported OpenAPI JSON/YAML files and examples here. Link to them from the top-level `docs/README.md`.
