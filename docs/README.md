# Docs

Project documentation and generated API docs.

Current state:
- Contains an `api/` subdirectory which can host OpenAPI or other API documentation.

How to use:
- Use `scripts/export_openapi.py` to generate or export API documentation if the backend exposes OpenAPI.
- Add high-level docs for running the project, configuration, and contribution guidelines.
